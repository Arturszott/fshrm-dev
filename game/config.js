'use strict';

var config = {
	// 20% of the screen height
	verticalCellSize: 0.2,
	// negative value mean background is scrolling top
	baseWaterSpeed: -40,
	// miliseconds
	GAME_TIME: 4000,
	accelerationSpeed: -3600,
	baseElementSpeed: -40,
}

module.exports = config;